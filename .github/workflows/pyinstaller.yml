name: PyInstaller

on: push

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Python
        uses: actions/setup-python@v1
        with:
          python-version: '3.8'
      - name: Install
        run: |
          python -m venv venv
          venv/Scripts/pip install pyinstaller
          venv/Scripts/pip install .
      - name: Make exe
        run: |
          echo "from cutadapt.__main__ import main" > script.py
          echo "sys.exit(main_cli())" >> script.py
          venv/Scripts/pyinstaller -F -w -n cutadapt script.py
      - name: Smoke test
        run: |
          dist/cutadapt.exe --version
          dist/cutadapt.exe -a TTAGACATATCTCCGTCG tests\data\small.fastq > result.fastq
          fc.exe tests\cut\small.fastq result.fastq
      - uses: actions/upload-artifact@v2
        with:
          name: cutadapt-exe
          path: dist/cutadapt.exe
